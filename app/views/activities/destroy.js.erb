var id = '<%= @review.id %>';
$('.review-like.' + id).html('<%= j render "activities/like",
  review: @review, activity: @activity %>');
$('.review-dislike.' + id).html('<%= j render "activities/dislike",
  review: @review, activity: @activity %>');
